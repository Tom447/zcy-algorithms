[664. 奇怪的打印机 - 力扣（LeetCode）](https://leetcode.cn/problems/strange-printer/description/)

**问题一：**

有台奇怪的打印机有以下两个特殊要求：

- 打印机每次只能打印由 同一个字符 组成的序列。

- 每次可以在从起始到结束的任意位置打印新字符，并且会覆盖掉原来已有的字符。

给你一个字符串 s ，你的任务是计算这个打印机打印它需要的最少打印次数。 

**示例 1：**

**输入：**s = "aaabbb"**输出：**2**解释：**首先打印 "aaa" 然后打印 "bbb"。

**示例 2：**

**输入：**s = "aba"**输出：**2**解释：**首先打印 "aaa" 然后在第二个位置打印 "b" 覆盖掉原来的字符 'a'。

 

**提示：**

- 1 <= s.length <= 100

- s 由小写英文字母组成

问题描述

![](images/WEBRESOURCE19f86698cf064f2981b0344ff7ca997dimage.png)

![](images/WEBRESOURCEc8d03293009f482bb35ee0c3ce2ce6dfimage.png)

思考方向

假设从边缘部分先打印后就不再动了

![](images/WEBRESOURCEea9ad55e7e4f4f41a4427498da8cbe86image.png)

![](images/WEBRESOURCE830a40ca3a7f476597fb123200c7b425image.png)

可能性的划分

![](images/WEBRESOURCE3f49925c560c42b6a9f55c401f2df9e4image.png)

如果 str[L] == str[k]，那么原本“左边打一次，右边打一次”的操作，可以**合并成一次**，从而**省掉一次打印**。

![](images/WEBRESOURCE5626bb4b133b4f548a5313368640a3feimage.png)

代码实现

```java
package class47;

public class code01_StrangePrinter {

    public static int strangePrinter1(String s){
        if (s == null || s.length() == 0) {
            return 0;
        }
        char[] str = s.toCharArray();
        return process1(str, 0, str.length - 1);
    }

    public static int process1(char[] str, int L, int R){
        if (L == R){
            return 1;
        }

        int ans = R - L + 1;
        for (int k = L + 1; k <= R; k++){
            ans = Math.min(ans, process1(str, L, k-1) + process1(str, k, R)
                    - (str[L] == str[k] ? 1 : 0));
        }
        return ans;
    }

    public static int strangePrinter2(String s) {
        if (s == null || s.length() == 0) {
            return 0;
        }
        char[] str = s.toCharArray();
        int N = str.length;
        int[][] dp = new int[N][N];
        return process2(str, 0, N - 1, dp);
    }

    public static int process2(char[] str, int L, int R, int[][] dp) {
        if (dp[L][R] != 0) {
            return dp[L][R];
        }
        int ans = R - L + 1;
        if (L == R) {
            ans = 1;
        } else {
            for (int k = L + 1; k <= R; k++) {
                ans = Math.min(ans, process2(str, L, k - 1, dp) + process2(str, k, R, dp) - (str[L] == str[k] ? 1 : 0));
            }
        }
        dp[L][R] = ans;
        return ans;
    }


    public static int strangePrinter3_modified(String s) {
        if (s == null || s.length() == 0) return 0;
        char[] str = s.toCharArray();
        int N = str.length;
        int[][] dp = new int[N][N];

        // 只需初始化长度为1的区间
        for (int i = 0; i < N; i++) {
            dp[i][i] = 1;
        }

        // 主循环：从长度为2开始填表
        for (int L = N - 2; L >= 0; L--) {        // L 从 N-2 开始
            for (int R = L + 1; R < N; R++) {     // R 从 L+1 开始（长度>=2）
                dp[L][R] = R - L + 1; // 最坏情况
                for (int k = L + 1; k <= R; k++) {
                    dp[L][R] = Math.min(dp[L][R],
                            dp[L][k-1] + dp[k][R] - (str[L] == str[k] ? 1 : 0)
                    );
                }
            }
        }
        return dp[0][N-1];
    }

    
}

```

**问题二：**

整型数组arr长度为n(3 <= n <= 10^4)，最初每个数字是<=200的正数且满足如下条件：

1. arr[0] <= arr[1]

1. arr[n-1] <= arr[n-2]

1. arr[i] <= max(arr[i-1], arr[i+1])

但是在arr有些数字丢失了，比如k位置的数字之前是正数，

丢失之后k位置的数字为0。

请你根据上述条件， 计算可能有多少种不同的arr可以满足以上条件。

比如 [6,0,9] 只有还原成 [6,9,9]满足全部三个条件，所以返回1种。

题目解析

![](images/WEBRESOURCE47f7da8d012b480c90561308c068bf64image.png)

![](images/WEBRESOURCEf696a470d81641538e7ea34cca3941c5image.png)

![](images/WEBRESOURCE168483eebbea4221b69abec54123c259image.png)

![](images/WEBRESOURCE7150952d4f534dc494ead9c09e2b279bimage.png)

![](images/WEBRESOURCE71ff93002f804bd4ac0ac1ed00f22d32image.png)

考虑边界的时候，只有当4位置的数比3位置的数小的时候，3位置的8如果达标与否就只取决于2位置的大小

这个时候2位置要满足条件就>=8才可以

![](images/WEBRESOURCE3390d0df697a4b0da5229439870ab16dimage.png)

上面是从6888才开始有效

边界为0的时候怎么填

![](images/WEBRESOURCEc052754c50e14a89af7221d94883e37eimage.png)

由于第8位置得到的数一定比7位置的数小，那么7位置的数就只能从1到200填一遍（题目默认每项都>0）

对于 arr[n-1] 丢失的情况：

- 它的候选值范围初始是 1~200（因为原始值是正整数且 ≤200）

- 它必须满足 arr[n-1] <= arr[n-2]

- 同时它会影响 arr[n-2] 是否满足条件3

- 所以在计数时，必须联合考虑 arr[n-2] 和 arr[n-3] 的值

因此，虽然 arr[n-1] 本身不直接受条件3约束，但**条件3 通过影响 ****arr[n-2]**** 而间接约束了 ****arr[n-1]**** 的可能取**

那么为什么要考虑8位置的数对7位置的数的影响呢？

这是因为，只有8位置的数小于7位置的数，才能保证7位置的数只受到6位置的数的影响，而使得所有子问题可以统一处理（同时满足条件arr[n-1] <= arr[n-2]，arr[i] <= max(arr[i-1], arr[i+1])）

考虑普遍位置的情况

“通过设定 arr[8] < arr[7]，我们可以确保 max(arr[6], arr[8]) 的值主要由 arr[6] 决定，从而使 arr[7] <= max(...) 的约束**退化为** arr[7] <= arr[6]，即还原了条件2。这样，所有位置都可以用同一个条件3的形式来处理，实现统一的状态转移。”

![](images/WEBRESOURCEc30570ea71814c7482c0a8959c4e2796image.png)

如果i位置不为0，那么i 9的决定就是无效的

考虑边界情况

![](images/WEBRESOURCEf1e83efef10b4ae08cc25278d3d10068image.png)

![](images/WEBRESOURCEf30562d87ab444d4bd926d9c7d54ad4bimage.png)

递归基准条件

如果原本i = 0 时arr[i] = 9, 那么f 13就是无效的

![](images/WEBRESOURCE7f29e3049988433796e52ae32eae94a4image.png)

可能性展开是在上游展开的，本层只负责此次的调用

只填一个点，而不是漫无目的的展开

![](images/WEBRESOURCE65c4228e814243fe94174c370813b573image.png)

为什么某个位置arr[i]的位置已经是！=0，为什么在做决定的时候v = n呢

从右往左，**把右边的值和关系当作“输入条件”**，
	向左传播约束，看左边有多少种方式能“接上”这个条件。

**v = n的操作类似于划定的点作为约束条件，使得整个尝试分支在此约束的条件下，走到头为止，是否可以得到有效的分支**

你可以把整个搜索过程想象成一棵**决策树**：

```
深色版本                    root
                   /    \
               v=1      v=2      ...      v=200   ← 枚举最后一个位置的可能值
               /          \                   \
           子问题       子问题               子问题
           (i-1)        (i-1)               (i-1)
```

- 每个节点代表一个调用：process1(i, v, s)

- 从根向下，是一条“假设路径”

- **v = n**** 就是在某个位置“钉死”一个值**，相当于说：

> “我假设 arr[i] = n，现在看看在这种前提下，前面能不能合法填完？”


为什么在暴力递归中 arr[index] = 0要回复现场

我理解了，其实就是需要将当前的选择撤销，然后赋予当前的选择新的值。核心逻辑是当前决策和其他决策共享同状态。比如i位置选择了v，那么arr状态数组中的i位置变成了v，那么当进行下一个决策的时候，i位置变成了s，那么arr中的i位置需要变成s，而i位置的v和s是两种不同的状态，所以为了不彼此干扰所以需要恢复现场

这是因为，递归撤销的条件

| 你的原话 | 对应的算法概念 | 说明 | 
| -- | -- | -- |
| “ | 撤销选择（Backtrack） | 每次尝试后必须“Undo” | 
| “ | 枚举决策（Enumerate Choices） | 尝试所有可能的值 | 
| “ | 共享可变状态（Shared Mutable State） | 数组、列表等是引用传递 | 


在第二种决策模式中

“这个 v 不是任意的，而是经过上一次决策的 pre 判断之后才成立的 v（并且在 s 的约束条件下）”

if (i == 0) { // 0...i 只剩一个数了，0...0

			return ((s == 0 || s == 1) && (arr[0] == 0 || v == arr[0])) ? 1 : 0;

		}

说的是这个v并不是随意的可能的都可以，而是在上一次决策中已经被约束了的

考虑可以优化的部分

![](images/WEBRESOURCE045a319f4ae643d8ab85b69dd039634bimage.png)

![](images/WEBRESOURCEf46d02f8b7754212bbf37d1f67864f11image.png)

s为1和0的时候是等价的

![](images/WEBRESOURCEa4a058d24e0b44c6b8f7b9d040d18701image.png)

![](images/WEBRESOURCE6cf009eb880c4782aa572746d319f3b7image.png)

代码书写的整理

如果s为2的时候，1..89这部分会没有

如果s = 0 或 s = 1的时候，1...89这部分就是有的

那么整理后就可以得到

![](images/WEBRESOURCEe10ed6a5bf174fba958f3fde4bdad7f8image.png)

![](images/WEBRESOURCEe1e58faf9e914703961cb0032047e5b5image.png)

```java
package class47;

public class code02_RestoreWays {


    public static boolean isValid(int[] arr){
        if (arr[0] > arr[1]) {
            return false;
        }
        if (arr[arr.length - 1] > arr[arr.length - 2]) {
            return false;
        }
        for (int i = 1; i < arr.length - 1; i++) {
            if (arr[i] > Math.max(arr[i - 1], arr[i + 1])) {
                return false;
            }
        }
        return true;
    }

    public static int ways(int[] arr){
        return process0(arr, 0);
    }

    public static int process0(int[] arr, int index){
        if (index == arr.length){
            return isValid(arr) ? 1 : 0;
        }else{
            if (arr[index] != 0){
                return process0(arr, index+1);
            }else{
                int ways = 0;
                for (int v = 1; v < 201; v++){
                    arr[index] = v;
                    ways += process0(arr, index + 1);
                }
                arr[index] = 0;
                return ways;
            }
        }
    }

    public static int ways1(int[] arr){
        int N = arr.length;
        if (arr[N - 1] != 0){
            return process1(arr, N - 1, arr[N - 1], 2);
        }else {
            int ways = 0;
            for (int v = 1; v < 201; v++){
                ways += process1(arr, N - 1, v, 2);
            }
            return ways;
        }
    }


    // 如果i位置的数字变成了v,
    // 并且arr[i]和arr[i+1]的关系为s，
    // s==0，代表arr[i] < arr[i+1] 右大
    // s==1，代表arr[i] == arr[i+1] 右=当前
    // s==2，代表arr[i] > arr[i+1] 右小
    // 返回0...i范围上有多少种有效的转化方式？
    public static int process1(int[] arr, int i, int v, int s){
        if (i == 0){// 0...i 只剩一个数了，0...0
            return
                    ((s == 0 || s == 1)
                            &&
                    (arr[0] == 0 || arr[0] == v)) ? 1 : 0;
        }
        // i > 0
        if(arr[i] != 0 && v != arr[i]){
            return 0;
        }
        // i>0 ，并且， i位置的数真的可以变成V，
        int ways = 0;
        if (s == 0 || s == 1){// [i] -> V <= [i+1]
            for (int pre = 1; pre < 201; pre++){
                ways += process1(arr, i - 1, pre, pre < v ? 0 : (pre == v ? 1 : 2));
            }
        }else{// ? 当前 > 右 当前 <= max{左，右}
            for (int pre = v; pre < 201; pre++){
                ways += process1(arr, i - 1, pre, pre == v ? 1 : 2);
            }
        }
        return ways;
    }

    public static int ways2(int[] arr){
        int N = arr.length;
        if (arr[N - 1] != 0){
            return zuo(arr, N - 1, arr[N - 1], 2);
        }else {
            int ways = 0;
            for (int v = 1; v < 201; v++){
                ways += zuo(arr, N - 1, v, 2);
            }
            return ways;
        }
    }

    public static int zuo(int[] arr, int i, int v, int s){
        if (i == 0){// 0...i 只剩一个数了，0...0
            return
                    ((s == 0 || s == 1)
                            &&
                            (arr[0] == 0 || arr[0] == v)) ? 1 : 0;
        }
        // i > 0
        if(arr[i] != 0 && v != arr[i]){
            return 0;
        }
        // i>0 ，并且， i位置的数真的可以变成V，
        int ways = 0;
        if (s == 0 || s == 1){// [i] -> V <= [i+1]
            for (int pre = 1; pre < v; pre++){
                ways += zuo(arr, i-1, pre, 0);
            }
        }
        ways += zuo(arr, i-1, v, 1);
        for (int pre = v + 1; pre < 201; pre++){
            ways += zuo(arr, i-1, pre, 2);
        }

        return ways;
    }

    public static int ways3(int[] arr){
        int N = arr.length;
        int[][][] dp = new int[N][201][3];
        if (arr[0] != 0){
            dp[0][arr[0]][0] = 1;
            dp[0][arr[0]][1] = 1;
        }else{
            for (int v = 1; v < 201; v++) {
                dp[0][v][0] = 1;
                dp[0][v][1] = 1;
            }
        }
        for (int i = 1; i < N; i++) {
            for (int v = 1; v < 201; v++) {
                for (int s = 0; s < 3; s++) {
                    if (arr[i] == 0 || v == arr[i]) {
                        if (s == 0 || s == 1) {
                            for (int pre = 1; pre < v; pre++) {
                                dp[i][v][s] += dp[i - 1][pre][0];
                            }
                        }
                        dp[i][v][s] += dp[i - 1][v][1];
                        for (int pre = v + 1; pre < 201; pre++) {
                            dp[i][v][s] += dp[i - 1][pre][2];
                        }
                    }
                }
            }
        }
        if (arr[N - 1] != 0) {
            return dp[N - 1][arr[N - 1]][2];
        } else {
            int ways = 0;
            for (int v = 1; v < 201; v++) {
                ways += dp[N - 1][v][2];
            }
            return ways;
        }
    }

    public static int ways4(int[] arr) {
        int N = arr.length;
        int[][][] dp = new int[N][201][3];
        if (arr[0] != 0) {
            dp[0][arr[0]][0] = 1;
            dp[0][arr[0]][1] = 1;
        } else {
            for (int v = 1; v < 201; v++) {
                dp[0][v][0] = 1;
                dp[0][v][1] = 1;
            }
        }
        int[][] presum = new int[201][3];
        for (int v = 1; v < 201; v++) {
            for (int s = 0; s < 3; s++) {
                presum[v][s] = presum[v - 1][s] + dp[0][v][s];
            }
        }
        for (int i = 1; i < N; i++) {
            for (int v = 1; v < 201; v++) {
                for (int s = 0; s < 3; s++) {
                    if (arr[i] == 0 || v == arr[i]) {
                        if (s == 0 || s == 1) {
                            dp[i][v][s] += sum(1, v - 1, 0, presum);
                        }
                        dp[i][v][s] += dp[i - 1][v][1];
                        dp[i][v][s] += sum(v + 1, 200, 2, presum);
                    }
                }
            }
            for (int v = 1; v < 201; v++) {
                for (int s = 0; s < 3; s++) {
                    presum[v][s] = presum[v - 1][s] + dp[i][v][s];
                }
            }
        }
        if (arr[N - 1] != 0) {
            return dp[N - 1][arr[N - 1]][2];
        } else {
            return sum(1, 200, 2, presum);
        }
    }

    public static int sum(int begin, int end, int relation, int[][] presum) {
        return presum[end][relation] - presum[begin - 1][relation];
    }

    // for test
    public static int[] generateRandomArray(int len) {
        int[] ans = new int[len];
        for (int i = 0; i < ans.length; i++) {
            if (Math.random() < 0.5) {
                ans[i] = 0;
            } else {
                ans[i] = (int) (Math.random() * 200) + 1;
            }
        }
        return ans;
    }

    // for test
    public static void printArray(int[] arr) {
        System.out.println("arr size : " + arr.length);
        for (int i = 0; i < arr.length; i++) {
            System.out.print(arr[i] + " ");
        }
        System.out.println();
    }

    public static void main(String[] args) {
        int len = 4;
        int testTime = 15;
        System.out.println("功能测试开始");
        for (int i = 0; i < testTime; i++) {
            int N = (int) (Math.random() * len) + 2;
            int[] arr = generateRandomArray(N);
            int ans1 = ways1(arr);
            int ans2 = ways2(arr);
            int ans3 = ways3(arr);
            int ans4 = ways4(arr);
            if (ans1 != ans2 || ans2 != ans3 || ans3 != ans4) {
                System.out.println("Oops!");
            }
        }
        System.out.println("功能测试结束");
        System.out.println("===========");
        int N = 100000;
        int[] arr = generateRandomArray(N);
        long begin = System.currentTimeMillis();
        ways3(arr);
        long end = System.currentTimeMillis();
        System.out.println("run time : " + (end - begin) + " ms");
    }

}

```

![](images/WEBRESOURCE73aecc4c664d4afa8bd2351be4dd77c0image.png)

相比较没有优化的部分，速度降低了200倍

**问题三： **// [https://lightoj.com/problem/internet-bandwidth](https://lightoj.com/problem/internet-bandwidth)

在互联网中，计算机（节点）之间有着丰富的连接，两个给定的节点之间可能存在多条路径。两个给定节点之间的总信息传输能力（带宽）是指单位时间内可以从一个节点传输到另一个节点的最大数据量。使用一种称为分组交换的技术，这些数据可以同时沿多条路径传输。

例如，下图显示了一个包含四个节点（用圆圈表示）的网络，它们之间总共有五条连接。每条连接都标有一个带宽值，表示其单位时间内的数据承载能力。

在我们的示例中，节点 1 和节点 4 之间的带宽是 25，这可以看作是路径 1-2-4 上的带宽 10、路径 1-3-4 上的带宽 10 和路径 1-2-3-4 上的带宽 5 的总和。没有其他节点 1 和节点 4 之间的路径组合能提供更大的带宽。

你必须编写一个程序，计算给定网络中两个指定节点之间的带宽，已知网络中所有连接的各自带宽。在这个问题中，假设连接的带宽在两个方向上总是相同的（在现实世界中不一定如此）。

输入

输入以一个整数 T（≤ 30）开始，表示测试用例的数量。

每个描述以一行包含一个整数 n（2 ≤ n ≤ 100）开始，表示网络中的节点数量。节点编号从 1 到 n。下一行包含三个数字 s、t 和 c。数字 s 和 t 是源节点和目标节点，数字 c（c ≤ 5000，s ≠ t）是网络中的连接总数。接下来是 c 行，描述这些连接。每行包含三个整数：前两个是连接的节点编号，第三个是连接的带宽。带宽是一个不超过 1000 的非负数。

一对节点之间可能存在多条连接，但一个节点不能与自身连接。所有连接都是双向的，即数据可以沿连接的两个方向传输，但两个方向上传输的数据总量必须小于带宽。

输出

对于每个输入用例，打印用例编号和源节点 s 与目标节点 t 之间的总带宽。

Sample

当然，以下是将示例输入和输出转换成文字格式的内容：

```
Sample

Input
2
4
1 4 5
1 2 20
1 3 10
2 3 5
2 4 10
3 4 20
4
1 4 2
1 4 20
1 4 20

Output
Case 1: 25
Case 2: 40

```

![](images/WEBRESOURCE585fc20d7a444a16bd3362e8cb98e1b8image.png)

这是个网络流问题

举例

![](images/WEBRESOURCE5699fc121055465493f3df7d1bf80966image.png)

深度优先遍历为什么不对

![](images/WEBRESOURCE3b2a249365464cf3b60e452db985dee3image.png)

因为其结果受到了深度优先遍历顺序的影响

![](images/WEBRESOURCEf0630d97459546abbae61cb7bb30f069image.png)

![](images/WEBRESOURCE5535426b75414c39b6a95345763fdb3fimage.png)

走abcd的时候，得到的结论是50

![](images/WEBRESOURCE24fb292d39d443c78496fca18cade3d2image.png)

而如果走abd和acd那么就可以得到100

![](images/WEBRESOURCEd2c7935a425741a19ca16edf4e2f02f0image.png)

加反向边

![](images/WEBRESOURCEe310fcd6bdad43f4925443f29819f3c9image.png)

![](images/WEBRESOURCE531bf0605c974854a614d2122983e5b8image.png)

补上反向边之后，不会影响让答案变大

![](images/WEBRESOURCEf20aa3ff001b481cbc87d543cea433e0image.png)

这里通过补上反向边，就可以得到正确答案

增加反向边不影响答案，但可以让错失的答案再次得到

蒂尼克算法

![](images/WEBRESOURCE58075400054d4953920f444642d29b4dimage.png)

先建立高度，通过这样的机制来避免来回走的问题

蒂尼克算法的另一个优化

![](images/WEBRESOURCE7da4b3a621614c1595a80211747cb9f2image.png)

![](images/WEBRESOURCE7a047a4a8743421d89e5725e709c986cimage.png)

用cur数组记录节点支路用过否的状态

![](images/WEBRESOURCE38ef7fa00aad419a98728f67aa6874d7image.png)

private ArrayList<ArrayList<Integer>> nexts;

是 1城市有哪些边  2城市有哪些边 这就是list套list

用多少，反向边增加多少

![](images/WEBRESOURCE2e7975fcbd594356b177a75547c9b260image.png)

反向边会在edge中注册

![](images/WEBRESOURCEaa300fdd7952430884e64c1904da21daimage.png)

![](images/WEBRESOURCEce5f8e59f49043abbcfc0f32868abcb6image.png)

城市拥有哪些边，是边号的意思，具体要找去edge中找

![](images/WEBRESOURCEfb74fd4451074787a5be05f813a0d61fimage.png)

0 和 1是反向边

2 和 3是反向边

4 和 5是反向边

反向边

![](images/WEBRESOURCE633f2661d4564ad592832182707f2577image.png)

可以由i ^ 1得到

![](images/WEBRESOURCE78879abc8cdf4be288b9674b949fef0aimage.png)

无向图要加两次

两条真实，两条隐含的

![](images/WEBRESOURCEfd8c08adec2445b68452718cf5fa89a7image.png)

其实在面对无向图的时候，ab 20 ba 20 也是可以的

上面这样做的目的是涵盖无向图和有向图而进行统一处理

每经过一次bfs就得到s到t是否可达，并且更新depth（每一次就算一遍)

![](images/WEBRESOURCE650827e8ba774240bea985b201efb4faimage.png)

![](images/WEBRESOURCE7309e273ae6b4d34a618de7b0e974732image.png)

![](images/WEBRESOURCE4edba462f7e34a368e2603a21e522937image.png)

cur[1] =3 说明0 1 2这些位置都不能走了，新走的时候需要从1号节点的地3条边走

![](images/WEBRESOURCEbac9bee24a68470eb91963f0874374faimage.png)

最后返回的ans 一定小于定于r

![](images/WEBRESOURCEf0f2a3763a2e4b8082c33b9d35589db2image.png)

当e点得到的任务r为0的时候，那么返回给d的完成的任务就是0

```java
package class47;

import java.util.ArrayList;
import java.util.Arrays;
import java.util.LinkedList;
import java.util.Scanner;

public class code03_DinicAlgorithm {

    public static class Edge{
        public int from;
        public int to;
        public int available;

        public Edge(int a, int b, int c){
            from = a;
            to = b;
            available = c;
        }
    }

    public static class Dinic{
        private int N;
        private ArrayList<ArrayList<Integer>> nexts;
        private ArrayList<Edge> edges;
        private int[] depth;
        private int[] cur;

        public Dinic(int nums){
            N = nums + 1;
            nexts = new ArrayList<>();
            for (int i = 0; i <= N; i++){
                nexts.add(new ArrayList<>());
            }
            edges = new ArrayList<>();
            depth = new int[N];
            cur = new int[N];
        }

        public void addEdge(int u, int v, int r){
            int m = edges.size();
            edges.add(new Edge(u, v, r));
            nexts.get(u).add(m);
            edges.add(new Edge(v, u, 0));
            nexts.get(v).add(m + 1);
        }

        private boolean bfs(int s, int t){
            LinkedList<Integer> queue = new LinkedList<>();
            queue.addFirst(s);
            boolean[] visited = new boolean[N];
            visited[s] = true;
            while (!queue.isEmpty()){
                int u = queue.pollLast();
                for (int i = 0; i < nexts.get(u).size(); i++){
                    Edge e = edges.get(nexts.get(u).get(i));
                    int v = e.to;
                    if (!visited[v] && e.available > 0){
                        visited[v] = true;
                        depth[v] = depth[u] + 1;
                        if (v == t){
                            break;
                        }
                        queue.add(v);
                    }
                }
            }
            return visited[t];
        }
        // 当前来到了s点，s可变
        // 最终目标是t，t固定参数
        // r，收到的任务
        // 收集到的流，作为结果返回，ans <= r
//        r 实际上代表的是当前节点 s 被分配的任务量，
//        也就是从 s 点出发最多可以推送多少流量到终点 t。
//        这个值是从父级调用传递下来的，表示上游节点希望 s
//            点能够向下游推送的最大可能的流。
        private int dfs(int s, int t, int r){
//            这个条件检查当前节点 s 是否已经是目标节点 t。
//            在递归过程中，当 dfs 调用达到目标节点 t 时，
//            意味着找到了一条从源点到汇点的有效路径。
//            如果 s == t 成立，说明已经到达了目的地，无需继续搜索。
//            r == 0:
//            这个条件检查剩余的任务量 r 是否为 0。
//            r 表示的是当前节点 s 可以尝试向下游推送的最大流量。
//            如果 r == 0，则表示没有更多的流量可以推送了。
//            当 r == 0 时，无论是否找到通往 t 的路径，
//            都不需要再继续尝试推送流量，因为已经达到了此次任务的最大容量。
            if (s == t || r == 0){
                return r;
            }
            int f = 0;
            int flow = 0;
            for (; cur[s] < nexts.get(s).size(); cur[s]++){
                int ei = nexts.get(s).get(cur[s]);
                Edge e = edges.get(ei);
                Edge o = edges.get(ei ^ 1);
                if (depth[e.to] == depth[s] + 1 && (f = dfs(e.to, t, Math.min(e.available, r))) != 0){
                    e.available -= f;
                    o.available += f;
                    flow += f;
                    r -= f;
                    if (r <= 0) {
                        break;
                    }
                }
            }
            return flow;
        }

        public int maxFlow(int s, int t) {
            int flow = 0;
            while (bfs(s, t)) {
                Arrays.fill(cur, 0);
                flow += dfs(s, t, Integer.MAX_VALUE);
                Arrays.fill(depth, 0);
            }
            return flow;
        }
    }

    public static void main(String[] args) {
        Scanner cin = new Scanner(System.in);
        int cases = cin.nextInt();
        for (int i = 1; i <= cases; i++) {
            int n = cin.nextInt();
            int s = cin.nextInt();
            int t = cin.nextInt();
            int m = cin.nextInt();
            Dinic dinic = new Dinic(n);
            for (int j = 0; j < m; j++) {
                int from = cin.nextInt();
                int to = cin.nextInt();
                int weight = cin.nextInt();
                dinic.addEdge(from, to, weight);
                dinic.addEdge(to, from, weight);
            }
            int ans = dinic.maxFlow(s, t);
            System.out.println("Case " + i + ": " + ans);
        }
        cin.close();
    }
}

```